import React, { FunctionComponent } from 'react';

export type FooterSectionProps = {
  // No Props
};

const FooterSection: FunctionComponent<FooterSectionProps> = (props) => {
  return <div className='py-36'></div>;
};

export default FooterSection;
